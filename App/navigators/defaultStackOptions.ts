import { textColor, shadow } from '../globalStyles'

export default {
  headerStyle: { backgroundColor: shadow, elevation: 0, shadowOpacity: 0, borderBottomWidth: 0 },
  headerTintColor: textColor,
  headerBackTitleVisible: false,
}
